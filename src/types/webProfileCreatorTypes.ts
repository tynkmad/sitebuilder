// src/types/webProfileCreatorTypes.ts

import { MainNavBarType } from 'rolex/skglobaltypes'; // Adjust the path as needed

export interface ExtendedMainNavBarType extends MainNavBarType {
    externalLink?: string; // Add any other custom properties here
}
